springdoc:
  api-docs:
    enabled: true 
    path: /user/v3/api-docs
  swagger-ui:
    enabled: true
    path: /user/swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: "/user"